import type { ReactNode } from 'react'

// 外部インタビュー画面: PublicHeader/Footer なしのフルスクリーンレイアウト
export default function ExternalInterviewLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
